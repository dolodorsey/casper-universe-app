import {StyleSheet,ViewStyle} from 'react-native';import {BlurView} from 'expo-blur';import {theme} from '@/lib/theme';type Props={children:React.ReactNode;style?:ViewStyle;intensity?:number};export default function GlassCard({children,style,intensity=80}:Props){return <BlurView intensity={intensity} tint="dark" style={[styles.glass,style]}>{children}</BlurView>}const styles=StyleSheet.create({glass:{borderRadius:theme.spacing.lg,borderWidth:1,borderColor:'rgba(255,255,255,0.1)',overflow:'hidden',backgroundColor:'rgba(0,0,0,0.4)'}});
